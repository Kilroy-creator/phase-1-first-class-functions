// 1. Function expression
const divide = function() {
  return 2000 / 100;
};

// 2. Arrow function (one param, square it)
const square = (x) => x * x;

// 3. Arrow function (two params, add them)
const add = (a, b) => a + b;
